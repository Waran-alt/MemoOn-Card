# =============================================================================
# ROOT – shared env (never commit .env)
# Copy to .env at project root. Backend and frontend load this when run locally.
# Docker Compose reads this file.
#
# Service-specific vars (JWT, CORS, API URL, E2E): see backend/env.example and
# frontend/env.example. Copy those to backend/.env and frontend/.env as needed.
# =============================================================================

NODE_ENV=development
FRONTEND_PORT=3002
BACKEND_PORT=4002
