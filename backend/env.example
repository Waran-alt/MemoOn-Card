# Optional overrides for backend (never commit .env).
# Primary values live in root .env; copy root env.example to root .env.
# Add or override only what you need here (e.g. POSTGRES_HOST=localhost when running backend alone).
