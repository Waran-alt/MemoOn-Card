<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="044-01-card-journey-source-without-study-events" author="memo">
        <comment>
            Historical journey rows used source study_events (ingestion from removed study_events table).
            Remap to ui and tighten CHECK so new rows cannot use study_events.
        </comment>
        <sql>
            UPDATE card_journey_events SET source = 'ui' WHERE source = 'study_events';
            ALTER TABLE card_journey_events DROP CONSTRAINT IF EXISTS chk_card_journey_source;
            ALTER TABLE card_journey_events ADD CONSTRAINT chk_card_journey_source
                CHECK (source IN ('ui', 'review_service', 'cards_route', 'decks_route'));
        </sql>
    </changeSet>

</databaseChangeLog>
