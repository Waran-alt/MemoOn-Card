# Optional overrides for frontend (never commit .env).
# Primary values live in root .env; copy root env.example to root .env.
# Add or override only what you need here (e.g. PORT=3002, NEXT_PUBLIC_API_URL).
